# This file is intentionally minimal.
# Railway services are configured per-directory via nixpacks.toml:
#   backend/ → backend/nixpacks.toml
#   frontend/ → frontend/nixpacks.toml
#
# In Railway dashboard: create two services from the same repo,
# set "Root Directory" to "backend" on one and "frontend" on the other.
